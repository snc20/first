pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo '🛠️ Build stage is running...'
            }
        }
        stage('Test') {
            steps {
                echo ' Tests passed!'
            }
        }
        stage('Deploy') {
            steps {
                echo ' Deploy completed!'
            }
        }
    }
}
